# Security Policy

## 支持范围

安全修复优先覆盖 `main` 上的最新代码和最新 GitHub Release。旧版本可能不再接收修复；请先升级到最新版本并确认问题仍然存在。

## 报告问题

请通过 GitHub Security Advisories 的私密报告入口提交安全问题：

<https://github.com/niocoders/boxkit/security/advisories/new>

如果该入口暂不可用，请联系维护者 `dev@boxkit.app`，邮件主题包含 `[security]`。请提供：

- 受影响的版本、平台和安装方式；
- 可重复的步骤或最小复现；
- 影响评估和已知利用条件；
- 在不包含真实凭据的前提下提供日志或截图。

请不要在公开 issue、pull request、日志、截图或插件包中发布访问令牌、私钥、用户数据或可直接利用的完整漏洞细节。维护者会在确认修复和披露时间后公开必要信息。

## 信任模型

插件视图为兼容模式，启用 Node 集成并关闭上下文隔离。权限清单和安装确认限制宿主 API 的授权范围，但不能把不受信任插件变成进程级沙箱。因此只安装可信来源的插件，并把插件包的来源、版本和 SHA-256 与发布清单核对。

市场和更新使用静态文件或 GitHub Releases；主仓库不运行市场后台，也不接收账户授权。发现伪造包、恶意市场清单或 Release 资产时，请按上述私密渠道报告。

## 凭据处理

开发密钥、签名证书、Apple 公证凭据和 GitHub token 只能保存在离线密钥管理系统或 CI Secrets 中。仓库不接受任何本机 token 路径、私钥例外规则或凭据样例。
